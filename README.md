# Neural-Style-Transfer
Use of CNNs to generate an image that combines the content of one image with the artistic style of another image.  Used the first 29 layers of pretrained VGG-19 to reconstruct content and style images, implemented via PyTorch.  Deployed the model using Streamlit for user-friendly interaction and visualization.
